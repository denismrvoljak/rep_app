
<h1> Your Workouts </h1>
<ul>
<% @workouts.each do |workout| %>
  <li><%= link_to workout.name, workout_path(workout) %> - <%= workout.date %></li>
<% end %>
</ul>
<%= link_to "Create new workout", new_workout_path, class: "button" %>
